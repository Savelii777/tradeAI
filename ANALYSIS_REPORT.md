# Полный анализ различий Лайв vs Бектест

## 📝 Простое объяснение проблемы с OBV

**Что такое OBV?**
- On-Balance Volume - индикатор объема, который суммирует объем в зависимости от направления цены
- Вычисляется через `cumsum()` - накопительная сумма от начала данных

**В чем была проблема?**
1. OBV использует `cumsum()` - это значит, что он суммирует от самого начала данных
2. В бектесте: CSV файлы могут содержать данные с 2017 года → OBV суммирует с 2017 года
3. В лайве: данные начинаются с последних 1500 свечей → OBV суммирует с другой точки
4. Результат: абсолютные значения OBV кардинально отличаются (например, -18000 vs +44000)

**Почему это важно?**
- OBV был исключен из фичей модели (правильно!)
- НО все еще вычислялся в коде - это создавало путаницу и лишние вычисления
- Хотя OBV не влиял на предсказания, это было несоответствие

**Что исправлено:**
- ✅ Удалено вычисление OBV из обоих файлов (live и train)
- ✅ Теперь OBV вообще не вычисляется - нет путаницы

**Важно понимать:**
- В лайве должны использоваться лайв данные (текущие) ✅
- В бектесте должны использоваться CSV данные (исторические) ✅
- Это правильно! Проблема была не в этом, а в том, что OBV вычислялся по-разному из-за разных начальных точек данных.

## 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ

### 1. OBV (On-Balance Volume) - ИСПРАВЛЕНО ✅

**Что было не так:**
- OBV вычислялся через `cumsum()` (накопительная сумма)
- `cumsum()` зависит от **начала окна данных** - откуда начинается подсчет
- В бектесте: CSV файлы могут содержать данные с 2017 года → cumsum начинается с 2017
- В лайве: данные начинаются с последних 1500 свечей → cumsum начинается с другой точки
- **Результат**: Абсолютные значения OBV кардинально отличались (разница 60000+)

**Почему это проблема:**
- OBV был исключен из фичей модели (правильно!)
- НО все еще вычислялся в `add_volume_features()` - лишние вычисления и путаница
- Хотя OBV не влиял на предсказания напрямую, это создавало несоответствие

**Что исправлено:**
- ✅ Удалено вычисление OBV из `add_volume_features()` в `live_trading_mexc_v8.py`
- ✅ Удалено вычисление OBV из `add_volume_features()` в `train_v3_dynamic.py`
- ✅ Убраны упоминания OBV из списка исключений (больше не нужно исключать то, чего нет)

### 2. Разные периоды данных

**Проблема:**
- В бектесте с `--reverse`: тест на данных 90-120 дней назад
- В лайве: используются текущие данные
- **Результат**: Модель обучена на старых данных, а тестируется на новых (или наоборот)

**Доказательство:**
```
Бектест последняя свеча: 2025-12-29 00:55:00
Лайв последняя свеча:    2026-01-05 21:40:00
Разница: 7 дней!
```

**Решение:**
- При сравнении использовать **одинаковые периоды**
- В лайве использовать те же данные, что и в бектесте

### 3. Все сигналы показывают SIDEWAYS - ГЛАВНАЯ ПРОБЛЕМА! 🔴

**Проблема:**
- В логах все сигналы отфильтровываются как SIDEWAYS
- Модель не предсказывает LONG или SHORT
- **Модель обучена на декабрьских данных (до 5 января 2026)**
- Сейчас 6 января 2026 - прошла неделя, рынок мог измениться

**Возможные причины:**
1. ✅ **ГЛАВНАЯ ПРИЧИНА**: Модель обучена на декабрьских данных, рынок изменился в январе
2. Фичи отличаются из-за OBV (исправлено)
3. Пороги слишком строгие (MIN_CONF=0.5, MIN_TIMING=0.8, MIN_STRENGTH=1.4)

**Доказательство из логов:**
```
→ SIDEWAYS | Conf: 0.77 | Timing: 2.85 ATR | Strength: 0.9
✗ Skipped (SIDEWAYS)
```

**Решение:**
1. 🔴 **КРИТИЧНО**: Переобучить модель на свежих данных (включая январь)
   ```bash
   python scripts/train_v3_dynamic.py --days 90 --test_days 30 --pairs 20 \
     --initial_balance 20 --output ./models/v8_improved --reverse --walk-forward
   ```
2. Проверить, почему модель всегда предсказывает SIDEWAYS после переобучения
3. Добавить диагностику в лайв для мониторинга

## 📊 ДЕТАЛЬНЫЙ АНАЛИЗ

### Загрузка данных

**Бектест:**
- Загружает из CSV файлов
- Фильтрует до target_date
- Берет последние LOOKBACK (1500) свечей

**Лайв:**
- Загружает через Binance API
- Берет последние LOOKBACK (1500) свечей
- Использует текущие данные

**Проблема:** Если CSV файлы содержат данные с 2017 года, то последние 1500 свечей в бектесте будут старыми данными!

### Вычисление фичей

**Найдено 135 фичей с различиями:**
- OBV: разница 385%
- OBV_SMA: разница 349%
- Цены: разница 6.5% (нормально, разные периоды)
- Объем: разница 52%

### Предсказания моделей

**Бектест:**
- Direction: SIDEWAYS (conf: 0.845)
- Timing: 2.674 ATR
- Strength: 0.97

**Лайв:**
- Direction: SIDEWAYS (conf: 0.808)
- Timing: 2.781 ATR
- Strength: 1.10

**Оба не проходят фильтры:**
- SIDEWAYS (dir_pred == 1)
- Strength < 1.4

## ✅ РЕКОМЕНДАЦИИ ПО ИСПРАВЛЕНИЮ

### 1. Исправить OBV

**Вариант A: Полностью удалить OBV**
```python
# В add_volume_features() - НЕ вычислять OBV вообще
# Уже исключен из фичей, но все еще вычисляется
```

**Вариант B: Использовать rolling OBV**
```python
# Вместо cumsum использовать rolling window
df['obv'] = (np.where(df['price_change'] > 0, df['volume'], -df['volume'])
             .rolling(window=100).sum())  # Rolling вместо cumsum
```

### 2. Синхронизировать данные

**В лайве использовать те же данные, что и в бектесте:**
- Загружать данные за тот же период
- Использовать те же временные метки

### 3. Проверить почему все SIDEWAYS

**Диагностика:**
1. Проверить распределение предсказаний модели
2. Сравнить фичи на одинаковом периоде
3. Проверить, не изменился ли рынок

**Возможные решения:**
1. Переобучить модель на свежих данных
2. Снизить пороги (но это рискованно)
3. Проверить, не переобучена ли модель

### 4. Улучшить логирование

**Добавить в лайв:**
- Логировать распределение предсказаний (сколько LONG/SHORT/SIDEWAYS)
- Логировать значения фичей для отладки
- Логировать почему сигналы отклоняются

## 🔧 КОНКРЕТНЫЕ ИСПРАВЛЕНИЯ

### Исправление 1: Удалить OBV из вычислений ✅ ИСПРАВЛЕНО

**Что сделано:**
- ✅ Удалено вычисление OBV из `add_volume_features()` в `live_trading_mexc_v8.py`
- ✅ Удалено вычисление OBV из `add_volume_features()` в `train_v3_dynamic.py`
- ✅ Убраны упоминания OBV из списка исключений (больше не нужно исключать то, чего нет)

**Результат:**
- OBV больше не вычисляется вообще
- Нет путаницы и лишних вычислений
- Код стал чище и понятнее

### Исправление 2: Синхронизировать данные

В `live_trading_mexc_v8.py`:

```python
# Вместо:
candles = binance.fetch_ohlcv(pair, tf, limit=LOOKBACK)

# Использовать:
# Загружать данные за тот же период, что и в бектесте
# Или убедиться, что CSV файлы обновлены до текущей даты
```

### Исправление 3: Добавить диагностику

В `live_trading_mexc_v8.py`:

```python
# После предсказаний:
dir_proba = models['direction'].predict_proba(X)
dir_conf = float(np.max(dir_proba))
dir_pred = int(np.argmax(dir_proba))

# Логировать распределение
logger.info(f"      Prediction distribution: "
           f"LONG={np.sum(np.argmax(dir_proba, axis=1) == 2)}, "
           f"SHORT={np.sum(np.argmax(dir_proba, axis=1) == 0)}, "
           f"SIDEWAYS={np.sum(np.argmax(dir_proba, axis=1) == 1)}")
```

## 📝 ВЫВОДЫ

1. ✅ **OBV - исправлено**: Удалено вычисление OBV из обоих файлов
2. **Разные периоды - это нормально**: 
   - В лайве используются текущие данные ✅
   - В бектесте используются исторические данные из CSV ✅
   - Это правильно! Проблема была в OBV, который вычислялся по-разному
3. **Все SIDEWAYS - ВОЗМОЖНАЯ ПРИЧИНА**: 
   - Модель обучена на **декабрьских данных** (до 5 января 2026)
   - Сейчас **6 января 2026** - прошла неделя
   - Рынок мог измениться, модель может быть не актуальна
   - **Решение**: Переобучить модель на свежих данных (последние 90 дней включая январь)
4. **Нужно**: 
   - Переобучить модель на свежих данных
   - Добавить диагностику в лайв
   - Проверить актуальность модели

## 🎯 ПЛАН ДЕЙСТВИЙ

1. ✅ Создан скрипт для анализа (`debug_live_backtest_full_analysis.py`)
2. ✅ Удалить OBV из вычислений - **ИСПРАВЛЕНО**
3. 🔴 **КРИТИЧНО**: Переобучить модель на свежих данных
   - Модель обучена на декабрьских данных (до 5 января)
   - Сейчас 6 января - прошла неделя
   - Рынок мог измениться, модель может быть не актуальна
   - **Команда**: `python scripts/train_v3_dynamic.py --days 90 --test_days 30 --pairs 20 --initial_balance 20 --output ./models/v8_improved --reverse --walk-forward`
4. ⏳ Добавить диагностику в лайв (логировать распределение предсказаний)

**Примечание:** 
- В лайве должны использоваться лайв данные (текущие) ✅
- В бектесте должны использоваться CSV данные (исторические) ✅
- Это правильно! Проблема была в OBV, который вычислялся по-разному из-за разных начальных точек данных.

